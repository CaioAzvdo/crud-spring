CREATE TABLE cursos (
                                        `_id` INT NOT NULL,
                                        `name` VARCHAR(200) NOT NULL,
                                        `categoria` VARCHAR(100) NULL,
                                        PRIMARY KEY (`_id`));
